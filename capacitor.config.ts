import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rumonge.rec',
  appName: 'REC Online',
  // Use the exported Next.js output directory for Capacitor web assets
  webDir: 'out'
};

export default config;
