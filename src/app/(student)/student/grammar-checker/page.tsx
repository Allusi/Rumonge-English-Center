import React from 'react';

// This page was replaced by `assignment-helper/page.tsx`.
// Provide a minimal module export so TypeScript and Next's build types work.
export default function Page() {
	return null;
}
